<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @activity["name"] %></title>
    <%= stylesheet '/style.css' %>
  </head>

  <body>

  <div class="banner-activity"style="background-image: url(<%=@activity["photo_url"]%>)" title="<%=@activity["name"]%>">
    <%# <div class="container"> %>
      <span class="title-name"><%= @activity["name"] %></span>
    <%# </div> %>
      <span class="title-category"><%= @activity["category"].upcase %></span>

  </div>

  <div class="card">
    <p>🌍 <%= @activity["city"] %></p>
    <p>🕒 <%= @activity["duration"] %> minutes</p>
    <p>👪 <%= @activity["participants_count"]%> participants</p>
    <p>💳 <%= @activity["price"] %> €</p>
    <a href="#" class="btn-book">Book a session</a>
  </div>

  <div class="card">
    <h4>💌 Comments from real customers:</h4>
    <span class="comment">"This is an activity you want to do before you die." (Jessica D.)</span>
    <span class="comment">"Best activity ever (I was not paid to write this)." (Daniel H.)</span>
    <h4>❤️ Satisfaction rate: 100%</h4>
  </div>

  </body>
</html>
